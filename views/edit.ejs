<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <title>Edit Task</title>
</head>
<body>
  <div class="container">
    <h1>Edit Task</h1>
    <form action="/tasks/update/<%= task._id %>" method="POST">
      <input type="text" name="title" value="<%= task.title %>" required>
      <textarea name="description" required><%= task.description %></textarea>
      <label for="dueDate">Select the Due date :</label>
      <input type="date" name="dueDate" value="<%= task.dueDate.toISOString().split('T')[0] %>" required>
      <button type="submit" class="update-task-btn">Update Task</button>
    </form>
    <a href="/tasks/<%= task._id %>" class="btn">Cancel</a>
  </div>
  <script src="/js/scripts.js"></script>
</body>
</html>
